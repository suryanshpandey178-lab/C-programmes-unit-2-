#include<stdio.h>
int main(){
    int arr[] = {10, 20, 30, 40, 50};
    int n = sizeof(arr)/sizeof(arr[0]);
    printf("Number of elements in the array: %d\n",n);
    int count = 0;
    {
        for(int i = 0; i < n; ++i)count++;
    printf("Count by loop: %d\n",count);
}
return 0;
}

